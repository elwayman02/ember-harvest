{{#harvest-picker-header
    prefix=prefix
    previous=(action 'previous')
    next=(action 'next')
}}
  {{displayYear.year}}
{{/harvest-picker-header}}
{{harvest-month-view
    prefix=prefix
    selectedMonth=selectedMonth
    select=(action 'select')
}}